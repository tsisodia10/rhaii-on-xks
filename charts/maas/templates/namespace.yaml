{{/* Main namespace (opendatahub) is created by helmfile createNamespace or KServe.
     Subscription namespace is only needed when maas-controller is present (3.4+).
     This template is intentionally empty for 3.3 compatibility. */}}
